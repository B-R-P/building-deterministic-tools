# Building Deterministic Tools

An agentic skill for building small, deterministic tools that solve repeated work — parsing datasets, wrapping APIs, consuming structured sources.

## Installation

```bash
npx skills add B-R-P/building-deterministic-tools
```


